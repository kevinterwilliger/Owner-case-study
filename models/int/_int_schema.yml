version: 2

models:
  - name: int_channel_spend
    description: Intermediate model containing channel spend data
    columns:
      - name: month_period
        tests:
          - not_null
      - name: surrogate_key
        tests:
          - not_null
          - unique
  - name: int_lead_value
    description: Intermediate model containing lead value calculations
    columns:
      - name: lead_id
        description: Unique identifier for the lead
        tests:
          - unique
          - not_null
